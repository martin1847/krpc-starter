{
  "name": "@krpc-starter/api",
  "version": "0.1.0",
  "private": true,
  "description": "Unified backend-access layer: krpc consumption (Hello MCP demo + generated Bookshelf client) + RpcResult envelope + TanStack Query bindings.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@krpc-starter/api-client": "workspace:*",
    "krpc-base": "^1.0.0"
  },
  "peerDependencies": {
    "@tanstack/react-query": "^5"
  },
  "devDependencies": {
    "@tanstack/react-query": "^5.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
